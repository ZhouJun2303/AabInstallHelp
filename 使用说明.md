# AAB 安装助手 使用说明

当前版本：**1.0.5**  
仓库：[https://github.com/ZhouJun2303/AabInstallHelp](https://github.com/ZhouJun2303/AabInstallHelp)  
发布页：[https://github.com/ZhouJun2303/AabInstallHelp/releases/latest](https://github.com/ZhouJun2303/AabInstallHelp/releases/latest)

本文面向实际使用者（测试、产品、运营、外包验收等），按「下载 → 准备环境 → 选包 → 安装」写完整操作。开发者打包、发版请看根目录 [README.md](README.md)。

---

## 1. 这是什么

商店、内测平台和不少构建流水线给出的是 **`.aab`（Android App Bundle）**，不能像 `.apk` 那样直接点开安装。

本工具把 `.aab` 按**当前设备**拆成可安装的 split APK，签上内置的 **Android debug 测试签名**，再装到手机上并尽量自动打开应用。

提供三个端，能力相同，只是入口不同：

| 端 | 安装对象 | 适用场景 |
| --- | --- | --- |
| Windows 桌面 | USB / adb 连上的 Android 设备 | 电脑上有 aab，手机开了 USB 调试 |
| macOS 桌面 | 同上 | 同上 |
| Android 本机 | 当前这台手机 / 平板 / 模拟器 | 文件已经在手机里，不想接电脑 |

界面名称是 **AAB 安装助手**。安装包文件名是 `AabInstalllHelp-…`（产品名里有三个 `l`，下载时请按文件名核对，不要改名后再用「检查更新」）。

---

## 2. 使用前必须知道的限制

请先读这一节，避免装完发现「装不上 / 覆盖不了商店包 / 不能拿去上架」。

1. **一律使用 debug 测试签名**  
   密钥是内置的 `debug.keystore`，别名 `androiddebugkey`，口令 `android`。桌面端和手机端用的是**同一套**测试身份。  
   **不能**覆盖 Google Play、应用宝、华为等商店的正式签名包。  
   **不能**当作上架包。装出来的应用只适合本地调试和内部验收。

2. **只能处理 `.aab`**  
   拖入或选择 `.apk`、`.apks`、压缩包都会被拒绝。请向研发要未加固、未加密的原始 `.aab`。

3. **桌面端必须能看到设备**  
   依赖内置 `adb`。手机要开 USB 调试，并且电脑上 `adb` 能列出该设备。无线调试只要已经出现在 `adb devices` 里也可以，本工具不会自己配对无线调试。

4. **手机端需要存储 + 安装未知应用权限**  
   没授完权不能扫描、也不能安装。

5. **大包在手机上可能内存不够**  
   转换 aab → apks 在本机进行。体积很大的 aab 在内存较小的真机上可能失败，请改用电脑端，或换内存更大的设备。

6. **应用内更新只认正式 GitHub Release**  
   检查更新读取 `releases/latest`。draft 不会作为更新源。Windows 下 `.exe`，macOS 下 `.dmg`，Android 下 `.apk`。下载后会按 `SHA256SUMS.txt` 校验，缺少校验文件会拒绝安装。

---

## 3. 下载哪个文件

打开 [最新 Release](https://github.com/ZhouJun2303/AabInstallHelp/releases/latest)，按你的系统下载对应资源，不要用仓库里的 `release/`、`android/app/build/` 中间产物。

| 你的环境 | 下载文件 | 说明 |
| --- | --- | --- |
| Windows 64 位 | `AabInstalllHelp-<版本>-windows-x64.exe` | NSIS 安装包，装完开始菜单里有「AabInstalllHelp」 |
| macOS（Intel / 通过 Rosetta 的 Apple Silicon） | `AabInstalllHelp-<版本>-macos.dmg` | 由 GitHub Actions 补传到同一 Release |
| Android 8.0 及以上（arm64 / x86_64） | `AabInstalllHelp-<版本>-android.apk` | 本机扫描并安装 aab |
| 校验 | `SHA256SUMS.txt` | 可选，手动核对下载完整性 |

手动校验示例（PowerShell）：

```powershell
Get-FileHash .\AabInstalllHelp-1.0.5-windows-x64.exe -Algorithm SHA256
```

把输出的哈希和 `SHA256SUMS.txt` 里同名那一行对比，应完全一致。

---

## 4. Windows 电脑端

### 4.1 安装本工具

1. 双击 `AabInstalllHelp-<版本>-windows-x64.exe`。
2. 按安装向导完成（可改安装目录）。
3. 从开始菜单或桌面快捷方式打开 **AabInstalllHelp**。
4. 首次打开若被 Windows Defender / 公司杀软拦截，选「仍要运行」。工具会启动内置 Java、adb、bundletool，部分环境会误报。

不需要单独安装 JDK、Android SDK、bundletool 或 adb，这些都打在安装包里。

### 4.2 把手机连上电脑

1. 用数据线连接手机和电脑。建议用原装或能稳定传数据的线，不要用「只能充电」的线。
2. 手机解锁，下拉通知栏，USB 用途选 **传输文件 / MTP**（不要停在「仅充电」）。
3. 打开 **开发者选项**：
   - 设置 → 关于手机 → 连续点击「版本号」约 7 次。
4. 打开 **USB 调试**：
   - 设置 → 系统 → 开发者选项 → USB 调试。
   - 部分品牌还要打开「USB 调试（安全设置）」或关闭「仅充电模式下禁止调试」。
5. 手机弹出「允许 USB 调试？」时勾选 **始终允许这台计算机**，点允许。
6. 回到本工具，点设备栏右侧 **刷新**。

下拉框应出现类似：

```
XXXXXXXX | 机型代号
```

左边是设备序列号，右边是 `adb` 读到的 product 名。

**多设备**：插了多台手机或同时开了模拟器时，在下拉框里选目标机，再点安装。当前选中的设备会写进日志。

**模拟器**：只要 `adb devices -l` 能列出、且带有 product 信息，就可以选。离线（offline）和名称为 Unknown 的条目会被跳过。

**驱动**：Windows 上部分机型需要厂商 USB 驱动（Google USB Driver、各品牌助手里的驱动）。设备管理器里若出现黄色感叹号，先把驱动装好再刷新。

### 4.3 选择 aab

三种方式，效果相同：

1. **拖放**：把 `.aab` 拖到「点击或将 aab 拖到这里选择文件」区域，松开。
2. **点击该区域**：弹出系统文件框，筛选器已设为 `*.aab`。
3. **候选文件下拉框**：工具会记住你上次选过的**目录**。该目录下按修改时间列出最多 100 个 `.aab`，最新的会标「（最新）」。点 **刷新目录** 可重新扫描这一层（不递归子目录）。

选中后，拖放区会显示「已选择文件：完整路径」。底部 **安装 AAB** 在「已选文件 + 已选设备」同时满足、且当前不在安装/更新中时才会点亮。

历史记录保存在本机用户数据目录的 `aab-history.json` 里，只记最后一次路径和所在目录，不会上传。

### 4.4 点「安装 AAB」之后会发生什么

底部按钮进入忙碌状态，日志区状态芯片变为 **安装中**。过程固定为 6 步，日志前缀都是 `[aab 安装]`：

| 步骤 | 界面日志在说什么 | 实际在做什么 |
| --- | --- | --- |
| 1 | 正在进行 aab 文件解析 | 用 bundletool 读清单，得到包名、versionName、versionCode、启动 Activity |
| 2 | 正在生成设备描述文件 | 按当前选中设备生成 `device-spec.json`（屏幕密度、语言、ABI 等） |
| 3 | 读取设备描述信息 | 把 spec 内容打到日志，便于核对是否连对设备 |
| 4 | 正在使用 debug 测试签名生成 apks | 按 spec 从 aab 抽出该设备需要的 split，并用 debug 密钥签名 |
| 5 | 正在将 apks 安装到设备中 | `bundletool install-apks` 推到手机 |
| 6 | 正在尝试自动启动应用 | `adb shell am start` 打开 MAIN/LAUNCHER Activity |

成功后弹出「温馨提示」：

- 能识别启动 Activity：提示已自动启动，可以开始验收。
- 不能识别：提示请手动打开应用。对话框里带包名和版本。

失败会弹出「安装出错」，可 **复制错误信息**。日志区状态芯片变为 **失败**。可用 **复制日志** 把完整过程交给研发。

安装期间不要拔线、不要锁屏到 USB 断开、不要再点一次安装（忙碌时按钮是禁用的）。

### 4.5 电脑端界面一览

- **标题栏右侧**
  - **检查更新**：问 GitHub 是否有更新的 Windows 包。
  - **关于（i）**：打开本项目 GitHub 页面。
  - 「有新版本」芯片：检查到更新后出现。
- **设备卡片**：当前连接设备 + 刷新。
- **安装包卡片**：候选列表、当前目录提示、拖放区。
- **日志卡片**：空闲 / 安装中 / 成功 / 失败；复制日志；清除日志。
- **底栏**：**安装 AAB**。

---

## 5. macOS 电脑端

操作与 Windows 相同，差异如下。

1. 打开 `AabInstalllHelp-<版本>-macos.dmg`，把应用拖到「应用程序」。
2. 首次打开若提示「来自身份不明的开发者」：系统设置 → 隐私与安全性 → 仍要打开。或右键应用 → 打开。
3. 用原装或认证过的数据线。新 Mac 只有 USB-C 时用转接头即可，关键是手机侧弹出 USB 调试授权。
4. 授权后在工具里点 **刷新**，选设备，拖入或选择 `.aab`，点 **安装 AAB**。

当前发布的 dmg 是 **x64**。Apple Silicon 可通过 Rosetta 运行。应用内更新会下载 macOS dmg；确认后会退出当前程序并打开安装包。

macOS 同样内置 Java、adb、bundletool，不需要再装 Android Studio，除非你本机 adb 端口被其它工具占用导致设备列表异常。出现这种情况时先关掉 Android Studio / 其它 adb 前端，再点刷新。

---

## 6. Android 手机端

手机端**不需要电脑、不需要 USB 调试**。把 `.aab` 弄到手机上（微信/飞书/钉钉/浏览器下载，或从电脑拷到 Download），用本 APK 扫描并安装。

系统要求：

- Android 8.0（API 26）及以上
- CPU：`arm64-v8a` 或 `x86_64`（常见真机和主流模拟器）

### 6.1 安装「AAB 安装助手」本身

1. 把 `AabInstalllHelp-<版本>-android.apk` 发到手机，用系统「文件」或浏览器打开。
2. 若提示「禁止未知来源」，允许该来源（浏览器 / 文件管理器 / 微信）安装。
3. 装好后桌面出现 **AAB 安装助手**。

### 6.2 首次启动：两步权限（必须做完）

未完成前不能扫描、不能安装。页面上有两张卡片，都要变成「已授权」：

**① 所有文件访问**

- 用途：扫描 Download、文档、微信/飞书/钉钉下载目录里的 `.aab`。
- 点 **去设置**，打开「允许访问所有文件」或同等开关。
- Android 10 及以下走的是存储读取权限，在应用详情里授予即可。

**② 安装未知应用**

- 用途：把拆出来的 APK 装到本机。
- 点 **去设置**，允许「AAB 安装助手」安装未知应用。

两步都好后点 **我已授权，继续**。若系统设置页返回后状态没刷新，再点一次该按钮。

部分国产系统会把「所有文件访问」藏在：设置 → 应用 → 特殊权限 → 所有文件访问权限。

### 6.3 找到 aab 并安装

进入主界面后会自动扫描。顶栏：

- **关于**：打开 GitHub 项目页
- **检查更新**：查 Android APK 新版本
- **刷新**：重新扫描存储

列表标题「本机 AAB」右侧有 **选择文件**。

**自动扫描会看哪些地方（按常见程度）：**

- 公共 Download / Downloads、Documents、Pictures、DCIM
- 浏览器子目录：Chrome、Edge、Firefox、各国产浏览器下载目录
- 微信：`tencent/MicroMsg/Download`、`WeiXin` 等；`Android/data/com.tencent.mm/...` 在部分系统上会被拦住
- 飞书 / Lark、钉钉对应下载目录
- 模拟器共享目录（如 MuMu `MuMuSharedFolder`、部分蓝叠共享路径）
- 系统 MediaStore 里登记过的 `*.aab`

结果按修改时间从新到旧。每条显示：文件名、可读路径、大小、时间；解析成功后还会显示 **包名、aab 版本、本机已装版本**。

**按钮文案会随本机情况变化：**

| 按钮 | 含义 |
| --- | --- |
| 安装 | 本机还没有这个包名 |
| 更新 | 本机已有，且 aab 的 versionCode **更高** |
| 再次安装 | 本机已有，versionCode **相同** |
| 降级 | 本机已有，且 aab 的 versionCode **更低**（工具会允许降级安装） |

点卡片可选中；点右侧按钮或底栏「安装/更新/… 文件名」开始安装。

**「选择文件」什么时候必须用：**

微信、QQ、部分系统把文件放在应用私有目录（`Android/data/...`），扫描扫不到。这时：

1. 点 **选择文件**
2. 用系统文件选择器进到对应应用的「文件」页，选出 `.aab`
3. 工具会拷到自己的缓存再加入列表

也可以在微信/文件管理器里对 `.aab` 点「用其他应用打开」，选 AAB 安装助手，效果等同选择文件。

### 6.4 安装页（手机竖屏）

开始安装后进入独立安装页，步骤芯片：

**解析 → 设备规格 → 生成 APKS → 安装 → 启动**

当前步骤为黄色，完成的为绿色。下方是滚动日志，可 **复制日志**。

- 进行中：底栏 **取消安装**；返回键也是取消。
- 结束：底栏 **返回列表**。成功会尝试自动打开刚装的应用。

平板（宽度 ≥ 600dp）为左右分栏：左边文件列表，右边日志，不必进单独安装页。

安装在前台服务中进行，通知栏会出现「正在安装 AAB」。不要在转换过程中强杀应用。

### 6.5 手机端更新本工具

权限就绪后会静默检查一次。有新版本时主列表上方出现横幅：

1. 文案类似「有新版本 vX.Y.Z」
2. 点 **下载并安装**
3. 进度条显示已下载大小
4. 校验 SHA256 通过后弹出系统安装框，确认即可

没有 `SHA256SUMS.txt` 或校验失败会保留旧版本，并提示原因。

---

## 7. 签名不一致（最常见的「装不上」）

设备上已经装着**同一个包名**，但签名不是本工具这套 debug 测试签名时，无法覆盖安装。典型来源：

- 应用宝 / Play / 公司正式包
- 研发用自己的 keystore 打的包
- 别人用另一套 debug 密钥装过

### 电脑端

弹出「签名不一致」：

- **卸载并重装**：adb 卸载旧应用（**应用数据会清空**），再装本次 aab。
- **复制错误信息**：把原始 `INSTALL_FAILED_UPDATE_INCOMPATIBLE` 等日志拷走。
- **取消**：停止，设备上仍是旧应用。

### 手机端

底部弹出同样三选项。选「卸载并重装」后系统可能再弹一次卸载确认，需要再点一次允许。

**不要对正式环境的账号数据点卸载**，除非你明确可以丢掉该应用的本地数据。

若旧包本来就是用本工具装的（同一套 debug 签名），更新 / 再次安装 / 降级都不会走这道确认。

---

## 8. 检查更新（三个端通用）

1. 点标题栏 **检查更新**（安装过程中按钮不可用）。
2. 已是最新：提示当前版本。
3. 有新版本：
   - **电脑**：弹出确认框。确认后下载对应平台安装包，校验 SHA256，然后**退出当前程序并拉起安装包**。下载中有进度（百分比和已下/总大小）。
   - **手机**：主列表横幅 → 下载并安装 → 系统安装框。
4. 网络不通、GitHub 被墙、Release 里缺少当前平台资源时，会提示失败原因或「未找到 Windows 安装包 / macOS dmg / Android 发布包」。

draft Release 不会出现在「最新」里。macOS dmg 有时会比 Windows/Android 晚几分钟（Actions 补传），那时检查更新可能暂时提示未找到 dmg，稍后再试。

---

## 9. 推荐工作流（给测试同学）

### 场景 A：研发把 aab 发到群里，电脑安装到真机

1. 把 aab 保存到电脑某个固定文件夹（例如 `D:\包\今日`）。
2. 手机开 USB 调试，插上，工具里刷新并选中该机。
3. 第一次把 aab 拖进工具；之后同一目录只需在「候选文件」里选「（最新）」。
4. 点安装，等自动启动，按测试用例验收。
5. 失败则点「复制日志」发回研发，不要只截一句「装不上」。

### 场景 B：出差或没有数据线，文件在微信里

1. 手机先装好 Android 端 APK 并授完两个权限。
2. 微信里把 aab **另存到手机**（存到「文件管理 / Download」比留在聊天记录更容易被扫到）。
3. 打开助手，没有出现就点 **选择文件** 从微信文件入口选。
4. 确认卡片上的包名、版本、按钮是「安装」还是「更新 / 降级」。
5. 点安装，等自动启动。

### 场景 C：覆盖安装正式包做对比

做不到。必须先卸载商店版（会丢数据），再装本工具签出来的测试包。对比包名可以相同，签名不同，系统视为两个互不覆盖的身份。

### 场景 D：要装回更老的测试包

手机端会显示 **降级**，点下去即可（仍必须是 debug 测试签名）。电脑端走 bundletool 安装，一般也可以装到较低 versionCode；若系统拒绝，先卸载再装。

---

## 10. 常见问题

### 电脑端下拉框是空的

按顺序查：

1. 线是否能传数据；换口、换线。
2. USB 用途是否为文件传输。
3. USB 调试是否打开，是否点过「允许这台计算机」。
4. 点 **刷新**。插上后有时要等 2～3 秒。
5. 关掉 Android Studio、其它 adb 工具后再刷新（adb 服务只能有一个）。
6. Windows 设备管理器里该手机是否正常。
7. 授权过期：关掉 USB 调试再打开，重新授权。

### 提示「未选择设备」或「未选择文件」

安装按钮亮起前必须：候选/拖放里有一个真实存在的 `.aab`，并且设备下拉框有选中项。文件被挪走或改名后，候选会提示「文件不存在，已从候选列表中移除」，重新选一次。

### 只支持 aab，研发给了 apk

直接用系统安装器或 `adb install` 即可，不必走本工具。本工具不做 apk 安装。

### 微信里明明有 aab，列表是空的

这是系统限制，不是漏扫。用 **选择文件**，或先把文件「保存到手机」到 Download 后再点刷新。

### 手机端提示内存不足 / OutOfMemory

日志会类似：

> 该 aab 体积过大，当前设备内存不足以在本机生成 apks。请改用电脑版安装，或换内存更大的设备/真机。

这是预期保护，换电脑端即可。

### 安装成功但没有自动打开

清单里没有标准 MAIN + LAUNCHER Activity（或被 disable）时无法自动启动。到桌面/应用抽屉里手动打开。电脑端对话框也会说明这一点。

### 装完打开闪退

常见原因：

- aab 里没有当前设备 ABI（例如只含 `armeabi-v7a`，而模拟器是 `x86_64`）。看日志里「AAB 包含 ABI / 本机偏好 / 本次选用」。
- 应用依赖 Google Play 服务、登录环境、特定渠道文件，测试签名包本身不包含这些。
- 需要先卸掉正式包（签名冲突时若强行留下旧数据，系统根本不会让你覆盖）。

把完整安装日志和 `adb logcat` 崩溃栈交给研发。

### 覆盖正式包失败

见第 7 节。只能卸载正式包后重装测试包。

### 检查更新失败

需要能访问 `https://api.github.com` 和 `https://github.com` 的下载域名。公司代理/防火墙拦截时，改为浏览器打开 Release 页手动下载。

### 电脑端杀软删除 java.exe / adb.exe

把安装目录加入白名单。这些是随包分发的运行时，不是病毒。

### 多个 aab 文件名相同

手机端会按「文件名 + 大小」去重，优先保留 Download 目录下的那份。电脑端只看你选中的那一个完整路径。

---

## 11. 日志里常见关键字

把日志发给研发时，可先自己对一下：

| 日志 / 错误 | 含义 | 你可以做的 |
| --- | --- | --- |
| `应用包名：…，应用版本信息：…` | 解析成功 | 核对是不是要测的那个包 |
| `启动 Activity：…` | 能自动打开 | 无 |
| `抱歉无法自动识别打开应用` | 清单无标准入口 | 手动打开 |
| `INSTALL_FAILED_UPDATE_INCOMPATIBLE` / `signatures do not match` | 签名不一致 | 按第 7 节卸载重装，或放弃覆盖正式包 |
| `生成设备描述文件出错` | adb 连不上或权限不够 | 重新授权 USB 调试、换线、刷新设备 |
| `生成 apks 文件出错` | aab 损坏、不是合法 bundle、磁盘满 | 重新下载 aab；电脑盘符留足空间 |
| `aab 文件不存在` | 路径失效 | 重新选择文件 |
| `该 aab 体积过大…` | 手机内存不够 | 改用电脑端 |
| `已取消` | 用户点了取消 | 需要时再装一次 |

电脑端完整日志可用 **复制日志**；手机端安装页同样有 **复制日志**。

---

## 12. 安全与隐私（使用者视角）

- aab 只在**本机**拆包、签名、安装，不会上传到任何服务器。
- 「检查更新」会访问 GitHub API 和 Release 资源，不携带 aab 内容。
- 电脑端会在用户数据目录记下「上次 aab 路径和目录」，便于下次列出候选。
- 安装结果带 debug 测试签名，**不要**把装出来的应用当正式包分发给外部用户，也**不要**用本工具处理含生产密钥的物料。

---

## 13. 从源码运行（可选）

给要改工具本身的人。日常装 aab 不需要这一节。

需要 [Node.js](https://nodejs.org/)。

```text
npm install
npm run start        # macOS
npm run start_win    # Windows
```

Android 工程在 `android/`，需要 Android SDK 与 JDK 17+。完整打包与发版见 [README.md](README.md) 和 [packages/README.md](packages/README.md)。

---

## 14. 操作核对清单（可打印）

**电脑装到真机**

- [ ] 已安装对应系统的 AAB 安装助手
- [ ] 手机已开开发者选项、USB 调试，并允许本电脑
- [ ] 工具里刷新后能看到设备
- [ ] 已选中正确设备（多机时务必看序列号）
- [ ] 已选中要测的 `.aab`，路径存在
- [ ] 已知这是测试签名，不能覆盖商店包
- [ ] 点安装，等到成功弹窗或自动启动
- [ ] 失败时复制完整日志，而不是只口头说「报错了」

**手机本机安装**

- [ ] 已安装 Android 端 APK（8.0+）
- [ ] 「所有文件访问」已授权
- [ ] 「安装未知应用」已授权
- [ ] 列表里能看到目标 aab，或已用「选择文件」选入
- [ ] 卡片上的包名、版本、按钮（安装/更新/降级）符合预期
- [ ] 签名冲突时清楚「卸载并重装」会丢应用数据
- [ ] 大包失败则改用电脑端
